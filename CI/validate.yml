---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: python
    tag: '3.8'

run:
  path: sh
  args:
  - -exc
  - |
    pip install -r requirements.txt
    inv format-code
