---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: hashicorp/terraform
    tag: '0.12.29'

inputs:
  - name: main-repo

run:
  path: sh
  args:
  - -exc
  - |
    ls ./main-repo
    cd ./main-repo
    pip install -r requirements.txt
    inv format-code
