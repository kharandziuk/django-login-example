---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: docker/compose

inputs:
  - name: main-repo
  - name: validate

run:
  path: sh
  args:
  - -exc
  - |
    cd ./main-repo
    pip install -r requirements.txt
    inv tests
