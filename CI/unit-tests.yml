---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: amidos/dcind

inputs:
  - name: main-repo


run:
  path: bash
  args:
  - -exc
  - |
    source /docker-lib.sh
    cd ./main-repo
    touch .env
    start_docker
    pip install -r requirements.txt
    APP_PORT=80 inv tests
